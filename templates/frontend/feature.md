# <Фича / область> — документация для фронтенда

> Живой справочный документ: текущее поведение бэкенда для фронтенд-разработчика и его AI.
> Без фронтенд-кода. Названия эндпоинтов, полей и enum-значений — на английском (это контракт).
> При изменениях правьте этот файл, а дельту фиксируйте в `ai/frontend/handoff/`.
> Связанный spec: `docs/specs/<file>.md`.

## Что это и зачем
<бизнес-цель: что пользователь получает и почему это нужно>

## Где в приложении
<какие экраны / разделы / потоки это затрагивает>

## Когда
<триггеры, предусловия, в каком состоянии доступно, для каких ролей>

## Как это работает (поток)
<шаги глазами пользователя и фронтенда: 1) … 2) … 3) …>

## API-контракт

Для каждого эндпоинта:

### `<METHOD> /api/<path>`
- **Назначение:** <…>
- **Доступ:** <роль / право; что без прав — 401/403>
- **Запрос** (query / body):

  | Поле | Тип | Обяз. | Правила / валидация | Примечание |
  |---|---|---|---|---|
  | … | … | … | … | … |

- **Ответ** (200/201): форма `JsonResource` — таблица полей или пример JSON-данных:

  ```json
  { }
  ```

- **Ошибки:** 422 (форма ошибок валидации), 403, 404, конфликт состояния и т.п.
- **Список:** пагинация / фильтры / сортировка (если применимо).

## Состояния и переходы
<если есть workflow: статусы, разрешённые переходы, что фронт показывает в каждом>

## Права и видимость
<что скрыто / показано по ролям; финансовые поля; контексты, где данные прячутся>

## Подводные камни
<что легко упустить и сломать UI: асинхронные состояния, пустые / ошибочные / загрузочные состояния,
порядок и идемпотентность вызовов, правила видимости. Флажим проактивно. Если нечего — «нет».>

## Как проверить (для фронта)
<наблюдаемые критерии приёмки глазами фронтенда>

## Допущения и открытые вопросы
<что предполагается; что надо уточнить у бэкенда / продукта>
